name: Using Reusable Workflow
on: 
  push:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Testing something
        run: echo "testing something"
  build:
    needs: test
    uses: /workspaces/gh-reusable-demo/.github/workflows/reusable.yml